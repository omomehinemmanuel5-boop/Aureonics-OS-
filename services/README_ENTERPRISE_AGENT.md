# LEX AUREON Enterprise Autonomous Revenue Orchestration

Run simulation:

```bash
python -m services.enterprise_agent.simulation
```

Run tests:

```bash
pytest -q tests/enterprise
```

Run via docker compose:

```bash
docker-compose up
```
